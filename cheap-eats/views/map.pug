extends layout

block content
    .inner
        h2 #{title}
        .map
            .autocomplete
                input.autocomplete__input(type='text', placeholder='the world is your $1 happy hour oyster', name='geolocate')
            #map
                p Loading...