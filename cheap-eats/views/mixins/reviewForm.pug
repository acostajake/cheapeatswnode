mixin reviewForm(restaurant)
    form(action=`/reviews/${restaurant._id}` method='POST')
    textarea(name='text' placeholder=`What should people know about ${restaurant.name}?`)
    .reviewer__meta
        .reviewer__stars
            each num in [5,4,3,2,1]
                input(type='radio' required id=`stars${num}` name='rating' value=num)
                label(for=`stars${num}`) #{num} Stars
            input.button(type='submit' value='Add Review')